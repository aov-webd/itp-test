export type AuthResult = {
    error: boolean,
    message: string,
    token: string
}
export type UserInfo = {
    accountType: string,
    displayName: string,
    displayName_lower: string,
    email: string,
    email_lower: string
}
